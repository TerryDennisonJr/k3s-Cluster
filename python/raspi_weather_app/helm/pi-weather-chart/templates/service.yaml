apiVersion: v1
kind: Service
metadata:
  name: {{ include "pi-weather-chart.fullname" . }}
  labels:
    {{- include "pi-weather-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pi-weather-chart.selectorLabels" . | nindent 4 }}
